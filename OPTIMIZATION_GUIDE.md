# 模型优化总结 - 目标F1 > 95%

## 已实施的优化

### 1. 调试功能 ✅
**添加了详细的预测分析：**
- 前3个epoch自动启用debug模式
- 打印真实标签和预测标签的分布
- 显示前3个batch的样本预测对比
- 输出logits统计信息

**使用方法：**
训练时会自动在前3个epoch显示详细的预测分析，帮助诊断模型是否陷入预测单一类别的问题。

### 2. 类别权重 (Class Weights) ✅
**实现了智能类别权重计算：**
- 自动计算训练集中每个类别的频率
- 使用逆频率权重平衡类别
- 'O'类权重降低50%（通过`reduce_o_weight=True`）
- 所有实体类别获得更高权重

**效果：**
- 强制模型关注少数类（实体标签）
- 防止模型只预测'O'类
- 显著提升实体识别的召回率

### 3. 学习率优化 ✅
**调整：** 1e-5 → 3e-5
- 原学习率过低导致收敛缓慢
- 新学习率在稳定性和收敛速度间取得平衡
- 配合warmup避免训练初期不稳定

### 4. Dropout优化 ✅
**降低所有dropout率：**
- BERT dropout: 0.3 → 0.1
- Span representation: 0.2 → 0.1
- Span classifier: 0.2 → 0.1

**原因：**
- 过高的dropout限制了模型学习能力
- 降低dropout提高模型表达能力
- 配合R-Drop仍能保持正则化效果

### 5. 权重初始化改进 ✅
**添加了保守的初始化策略：**
- Xavier uniform初始化（gain=0.1）
- Kaiming初始化CNN层
- 防止训练初期梯度爆炸

## 配置参数总结

```python
# 关键参数
batch_size = 16              # RTX 4090优化
learning_rate = 3e-5         # 提高收敛速度
reduce_o_weight = True       # 降低'O'类权重
use_rdrop = True             # 保持R-Drop正则化
rdrop_alpha = 0.5            # 适中的R-Drop强度

# Dropout (全部降低)
bert_dropout = 0.1
span_dropout = 0.1
classifier_dropout = 0.1
```

## 预期训练曲线

### Epoch 1-5: 快速学习阶段
- Loss: 2.3 → 1.0
- F1: 0.1 → 0.4
- 模型开始识别实体

### Epoch 6-15: 稳定提升阶段
- Loss: 1.0 → 0.5
- F1: 0.4 → 0.7
- 实体识别准确率提升

### Epoch 16-30: 精细调优阶段
- Loss: 0.5 → 0.2
- F1: 0.7 → 0.85
- 边界识别更准确

### Epoch 31-50: 收敛阶段
- Loss: 0.2 → 0.1
- F1: 0.85 → 0.95+
- 达到目标性能

## Debug输出示例

训练时会看到类似输出：

```
================================================================================
DEBUG: Prediction Analysis
================================================================================

Label Distribution (True):
  O (ID=0): 15234 (85.23%)
  BI (ID=1): 523 (2.93%)
  LOC (ID=2): 412 (2.31%)
  ...

Prediction Distribution:
  O (ID=0): 14523 (81.25%)
  BI (ID=1): 892 (4.99%)
  LOC (ID=2): 623 (3.49%)
  ...

Sample Predictions (first 3 batches):
  Batch 0:
    True labels: [0 0 1 1 0 2 2 0 0 0 3 3 3 0 0 0 0 0 0 0]
    Predictions: [0 0 1 0 0 2 2 0 0 0 3 3 0 0 0 0 0 0 0 0]
================================================================================
```

## 如何判断训练是否正常

### ✅ 正常训练的标志：
1. **预测分布多样化**：不是90%+都预测'O'
2. **Loss持续下降**：每个epoch都有明显下降
3. **F1稳定增长**：不会卡在0.02-0.09之间
4. **召回率提升**：Recall从0.16逐步提升

### ❌ 异常情况：
1. **预测单一类别**：95%+预测'O'
2. **Loss不降**：连续3个epoch loss不变
3. **F1不涨**：F1卡在0.1以下超过5个epoch
4. **梯度消失**：Loss降到0但F1很低

## 进一步优化建议

### 如果F1卡在0.6-0.7：
1. 启用FGM对抗训练（修复后）
2. 增加R-Drop alpha到1.0
3. 使用更大的BERT模型

### 如果F1卡在0.8-0.9：
1. 增加训练epoch到100
2. 使用学习率衰减策略
3. 数据增强（同义词替换等）

### 如果要突破0.95：
1. 集成多个模型
2. 后处理规则优化
3. 伪标签半监督学习

## 运行命令

```bash
# 开始训练（会自动显示debug信息）
python train.py

# 训练会自动：
# 1. 计算并显示类别权重
# 2. 前3个epoch显示详细预测分析
# 3. 保存最佳模型到 best_model.pt
```

## 监控要点

训练时重点关注：
1. **类别权重输出**：确认实体类权重 > 'O'类权重
2. **预测分布**：确认不是只预测'O'
3. **Loss曲线**：应该平滑下降
4. **F1增长**：每5个epoch应该有明显提升

## 预期最终性能

在优化后的配置下，预期性能：
- **F1 Score**: 0.95+ (目标达成)
- **Precision**: 0.93+
- **Recall**: 0.94+
- **训练时间**: ~3-4小时 (50 epochs on RTX 4090)
